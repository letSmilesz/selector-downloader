1. engine/reader.py (download_chapter_page): убрать хардкод семейства —
   дефолтные селекторы "select.page-selector", "span.pages-count",
   "div.manga-img-placeholder[data-page]" вынести в роли пресета.
   Движок должен остаться сайт-агностиком.
2. grouple.json: заменить nth-child-селекторы на стабильные
   (settings_button, web/page_mode_toggle, loading_indicator,
   next_chapter_btn "... a:nth-child(3)") — должок перед правилами full.md.
3. ensure_mode: (а) проверка текущего режима ДО переключения — если режим
   уже нужный, не открывать настройки и не кликать; (б) проверка, что режим
   реально применён, после каждого перехода/релоада/ретрая.
4. Сайты, открывающие главу в новой вкладке (target=_blank): сейчас
   _on_new_page в driver.py закрывает все вкладки кроме основной.
   Текущим поддерживаемым сайтам не нужно; понадобится — доработать.
5. Чистка буферов .tmp_ranobe_* после успешной сборки EPUB (сейчас копятся).
6. Поправить docstring scroll_and_load_images: функция возвращает 3 значения
   (final_count, unloaded, src_index), в докстринге — 2.
7. CLI: не печатать служебные JSON-события emit_event в консоль,
   когда запуск без GUI (или визуально отделить).
8. GUI: ограничить длину log_list (длинные прогоны с --debug раздувают память).
9. (опционально) Переименовать grouple.json и поле "name": "grouple"
   в нейтральное (например, "readmanga-family"), чтобы ничто не указывало
   на источник селекторов.